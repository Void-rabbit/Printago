<Window x:Class="PrintagoManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrintagoManager"
        xmlns:viewModels="clr-namespace:PrintagoManager.ViewModels"
                xmlns:views="clr-namespace:PrintagoManager.Views"
        mc:Ignorable="d"
        Title="Printago Manager" Height="600" Width="900">
    <Window.Resources>
        <!-- Basic Dark Theme Styles (can be expanded or moved to App.xaml) -->
                <!-- Styles will be moved to App.xaml later, this is just for current step -->
        <Style TargetType="Grid" x:Key="MainGridStyle">
            <Setter Property="Background" Value="#2D2D30"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#3D3D3E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#3D3D3E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/> <!-- Printago Blue Accent -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Style="{StaticResource MainGridStyle}">
        <!-- DataContext for LoginViewModel will be set in code-behind for the Window -->
        <!-- This ensures LoginViewModel is available for bindings on LoginPanel and MainContentPanel if needed -->

        <StackPanel x:Name="LoginPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Bambu Lab Login" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            <TextBlock Text="Email:"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
            <TextBlock Text="Password:"/>
            <PasswordBox x:Name="UserPasswordBox" Margin="0,0,0,10"/>
            <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=UserPasswordBox}" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding LoginStatus}" Foreground="OrangeRed" Margin="0,10,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <Grid x:Name="MainContentPanel" Visibility="Collapsed">
            <views:PrintagoPrintersView/>
        </Grid>
    </Grid>
</Window>
